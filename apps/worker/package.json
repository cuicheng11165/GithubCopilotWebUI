{
  "name": "@app/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsup src/worker.ts --format esm --sourcemap --clean",
    "start": "node dist/worker.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@app/contracts": "workspace:*",
    "@app/db": "workspace:*",
    "@app/logging": "workspace:*",
    "@app/repository-tools": "workspace:*",
    "@github/copilot-sdk": "1.0.7",
    "zod": "3.25.64"
  },
  "devDependencies": {
    "tsup": "8.5.0"
  }
}
