{
  "name": "copilot-web-ui",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "corepack pnpm db:generate",
    "build": "corepack pnpm -r build",
    "dev": "node scripts/dev.mjs",
    "start:local": "node scripts/start-local.mjs",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "corepack pnpm --filter @app/db generate",
    "db:migrate": "corepack pnpm --filter @app/db migrate",
    "db:migrate:dev": "corepack pnpm --filter @app/db exec node scripts/sqlite-prisma.mjs migrate dev",
    "db:validate": "corepack pnpm --filter @app/db validate"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "tsx": "4.20.3",
    "typescript": "5.8.3",
    "vitest": "3.2.4"
  }
}
