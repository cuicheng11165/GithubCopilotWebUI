{
  "name": "@app/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate": "prisma generate --schema prisma/schema.prisma && prisma generate --schema prisma-sqlite/schema.prisma",
    "migrate:local": "node scripts/sqlite-prisma.mjs migrate deploy",
    "migrate:multi-user": "prisma migrate deploy --schema prisma/schema.prisma",
    "validate": "node scripts/validate.mjs",
    "prisma": "prisma"
  },
  "dependencies": {
    "@prisma/client": "6.19.0"
  },
  "devDependencies": {
    "prisma": "6.19.0"
  }
}
